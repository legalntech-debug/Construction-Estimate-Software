/**
 * Canonical CAD floor-plan renderer entry point.
 * The existing CadFloorPlansView remains the implementation during renderer migration.
 */
export { default } from '../components/CadFloorPlansView';
